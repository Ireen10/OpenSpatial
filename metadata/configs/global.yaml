output_root: metadata_out
scale: 1000
batch_size: 3
num_workers: 2
resume: false
strict: true

# Global QA task registry (shared across datasets).
# Can be overridden by CLI --qa-config.
qa_config: metadata/configs/qa_tasks.yaml


# 测试方案（Test Plan）— 待定主题

> 本文件由 `metadata/plans/templates/test_plan.md` 拷贝。**须在 `plan.md` 对齐之后**再填写用例并与 `plan.md` 任务项逐条映射。

## 测试方案（Test Plan）

### 测试范围

- 覆盖范围：
- 不覆盖范围：

### 单元测试

- 用例列表：
- 输入构造：
- 断言点：

### 集成测试

- 与 OpenSpatial 的对接点：
- 需要的样例 Parquet / JSON：
- 预期输出与检查方式：

### 质量门槛（Gate）

- 通过条件：（建议继续包含：仓库根 `python -m pytest metadata/tests -q`，Linux 与本地均适用）
- 失败时如何定位：
